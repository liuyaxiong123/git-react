字符串操作：
声明：
两种写法：
'abc'   new String ('abc')
str.lastIndexOf('内容'，起始位置):检索某个内容在字符串中最后一次出现的位置，没有返回-1，可以有第二个参数   从哪个位置开始
str.slice(起始位置，结束位置):做字符串截取
str.replace(被替换内容，要提换的内容):替换字符串内容
str.concat():合并字符串
str.trim():去除字符串首尾空白字符(IE9以下低版本浏览器不兼容)